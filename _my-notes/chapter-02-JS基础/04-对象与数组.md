# 04 - 对象与数组

> 🎯 **学习目标**：掌握对象和数组的高级操作。

---

## 一、对象基础

```javascript
const person = {
    name: 'Alice',
    age: 25,
    greet() {
        console.log(`Hi, I'm ${this.name}`);
    }
};

// 访问属性
person.name;      // 'Alice'
person['age'];    // 25
```

---

## 二、解构赋值

```javascript
// 对象解构
const { name, age } = person;

// 数组解构
const [first, second] = [1, 2, 3];

// 嵌套解构
const { address: { city } } = { address: { city: 'Beijing' } };
```

---

## 三、展开运算符

```javascript
// 数组展开
const arr1 = [1, 2];
const arr2 = [...arr1, 3, 4]; // [1, 2, 3, 4]

// 对象展开
const obj1 = { a: 1 };
const obj2 = { ...obj1, b: 2 }; // { a: 1, b: 2 }
```

---

## 四、数组高级方法

```javascript
const numbers = [1, 2, 3, 4, 5];

// reduce - 归约
const sum = numbers.reduce((acc, num) => acc + num, 0);

// find - 查找
const found = numbers.find(num => num > 3);

// includes - 包含
const hasThree = numbers.includes(3);
```

---

## ❓ 我的疑问

> 在此处记录学习过程中遇到的问题和困惑...

1. 
2. 
3. 

---

## 与原课程对照

| 本文件内容 | 对应原课程位置 |
|:---|:---|
| 数组方法 | `2-js-basics/4-arrays-loops/README.md` |

---

<!-- STEP1-DONE: 2026-04-07 -->
