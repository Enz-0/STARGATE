# 02 - 事件监听

<!-- STEP1-DONE: 2026-04-07 -->

## 📚 本节内容

### addEventListener 基础

```javascript
const button = document.querySelector('#myButton');

// 基本语法
button.addEventListener('click', function(event) {
    console.log('按钮被点击了！');
});

// 使用箭头函数
button.addEventListener('click', (e) => {
    console.log('事件对象:', e);
});
```

### 常用事件类型

| 事件 | 触发时机 |
|------|----------|
| `click` | 鼠标点击 |
| `dblclick` | 双击 |
| `mouseenter` | 鼠标移入 |
| `mouseleave` | 鼠标移出 |
| `input` | 输入框内容变化 |
| `change` | 值改变（失焦后触发）|
| `submit` | 表单提交 |
| `keydown` | 键盘按下 |
| `load` | 资源加载完成 |

### 事件对象

```javascript
element.addEventListener('click', (event) => {
    event.target;        // 触发事件的元素
    event.type;          // 事件类型
    event.preventDefault(); // 阻止默认行为
    event.stopPropagation(); // 阻止冒泡
});
```

### 移除事件监听

```javascript
function handleClick() {
    console.log('clicked');
}

button.addEventListener('click', handleClick);
button.removeEventListener('click', handleClick);
```

## ❓ 我的疑问

_（留白供后续学习时记录）_



## 与原课程对照

| 内容 | 原课程位置 | 备注 |
|------|------------|------|
| addEventListener | 3-terrarium/2-intro-to-html | 事件绑定 |
| 事件对象 | 3-terrarium/3-intro-to-DOM | event 参数 |

