# 03 - 元素操作

<!-- STEP1-DONE: 2026-04-07 -->

## 📚 本节内容

### createElement - 创建新元素

```javascript
// 创建新元素
const newDiv = document.createElement('div');
newDiv.textContent = 'Hello World';
newDiv.id = 'myDiv';
newDiv.className = 'container';

// 添加到 DOM
document.body.appendChild(newDiv);
```

### classList - 类名操作

```javascript
const element = document.querySelector('.box');

// 添加类名
element.classList.add('active');

// 移除类名
element.classList.remove('hidden');

// 切换类名（有则移除，无则添加）
element.classList.toggle('visible');

// 判断是否包含类名
if (element.classList.contains('active')) {
    console.log('包含 active 类');
}
```

### innerHTML vs textContent

```javascript
const container = document.querySelector('#container');

// innerHTML - 解析 HTML 标签
container.innerHTML = '<strong>加粗文本</strong>';

// textContent - 纯文本，不解析 HTML
container.textContent = '<strong>纯文本显示</strong>';

// 安全性注意：避免使用 innerHTML 插入用户输入内容
```

### 其他常用操作

```javascript
// 设置属性
element.setAttribute('data-id', '123');

// 获取属性
const id = element.getAttribute('data-id');

// 移除属性
element.removeAttribute('disabled');

// 修改样式
element.style.backgroundColor = 'blue';
element.style.fontSize = '16px';
```

## ❓ 我的疑问

_（留白供后续学习时记录）_



## 与原课程对照

| 内容 | 原课程位置 | 备注 |
|------|------------|------|
| createElement | 3-terrarium/3-intro-to-DOM | 动态创建元素 |
| classList | 3-terrarium/2-intro-to-html | 类名管理 |
| innerHTML | 3-terrarium/1-intro-to-DOM | 内容插入 |

